# Optional absolute backend base URL for server-side fetches.
# Browser requests go through the local Next proxy on http://localhost:3001.
NEXT_PUBLIC_BACKEND_URL=https://localhost:4000
GATEWAY_INTERNAL_URL=https://localhost:4000
